================================================================================
  Luxury Stays — Demo Login Credentials (LOCAL DEV ONLY)
================================================================================

These are the seed accounts created by `npm run seed` (see scripts/seed.ts).
They exist only in your local development database. They are NOT production
secrets — every account uses the same demo password.

Real secrets (AUTH_SECRET, Xendit keys, DATABASE_URL) live in `.env`, which is
git-ignored and must never be committed.

Password for ALL accounts below:  password123
App URL (dev):                     http://localhost:3000
Sign in at:                        http://localhost:3000/login

--------------------------------------------------------------------------------
  PLATFORM ADMIN  ->  /admin
--------------------------------------------------------------------------------
  Email:  admin@example.com
  Role:   admin
  Can:    view platform stats + gross revenue, list/suspend/activate tenants,
          provision new hotels, see all bookings across every hotel.

--------------------------------------------------------------------------------
  HOTEL OWNERS (tenant dashboard)  ->  /dashboard
--------------------------------------------------------------------------------
  The Grand Azure (Seminyak, Bali)
    Email:  owner.thegrandazure@example.com
    Role:   tenant_owner

  Meridien Heights (Jakarta)
    Email:  owner.meridienheights@example.com
    Role:   tenant_owner

  Sante Lagoon Resort (Lombok)
    Email:  owner.santelagoonresort@example.com
    Role:   tenant_owner

  Each owner can: edit their hotel profile, manage rooms & rates, and view /
  cancel their hotel's bookings. Owners only see their own hotel.

--------------------------------------------------------------------------------
  GUEST (booking side)  ->  /account
--------------------------------------------------------------------------------
  Email:  guest@example.com
  Role:   guest
  Can:    browse hotels, book rooms, pay (Xendit mock locally), view own bookings.

--------------------------------------------------------------------------------
  Notes
--------------------------------------------------------------------------------
  - New hotels provisioned via the admin "Add a hotel" form create their own
    tenant_owner account with whatever email/password the admin sets there.
  - Re-running `npm run seed` re-creates these accounts (and resets tenant data).
================================================================================
